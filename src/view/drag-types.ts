/** Custom MIME type for dragging a thread row onto a mailbox in the folder
 *  list — a plain "text/plain" payload would also accept unrelated drags
 *  (OS file/text drops) onto a mailbox row. */
export const THREAD_DRAG_TYPE = "application/x-oe-thread-id";
